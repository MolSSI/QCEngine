name: test
channels:
  - conda-forge
dependencies:

    # Core
  - python
  - pip
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental >=0.4.0
  - pydantic >=0.20.0

    # Testing
  - pytest
  - pytest-cov
  - codecov
