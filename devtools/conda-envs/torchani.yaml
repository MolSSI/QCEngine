name: test
channels:
  - conda-forge
dependencies:

    # Core
  - python
  - pip
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental >=0.12.0
  - pydantic>=1.0.0

  - pytorch::pytorch-nightly-cpu

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pip:
    - torchani
