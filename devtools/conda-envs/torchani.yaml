name: test
channels:
  - conda-forge
  - pytorch-nightly
dependencies:

    # Core
  - python
  - pip
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental >=0.12.0
  - pydantic>=2.1
  - pydantic-settings

  - pytorch

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pip:
    - torchani
