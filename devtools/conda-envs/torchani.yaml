name: test
channels:
  - conda-forge
  - pytorch-nightly
dependencies:

    # Core
  - python
  - pip
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental >=0.12.0
  - pydantic>=2
  - pydantic-settings

  - pytorch
  - geometric

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - pip:
    - torchani
