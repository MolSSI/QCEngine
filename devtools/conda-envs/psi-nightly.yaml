name: test
channels:
  - defaults
  - psi4/label/dev
  - conda-forge
dependencies:
  - psi4=1.6
  - blas=*=mkl  # not needed but an example of disuading solver from openblas and old psi
  - numpy=1

    # Core
  - python
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental >=0.26.0
  - pydantic>=2.0.0
  - pydantic-settings
  - msgpack-python
  - numpy=1

    # Testing
  - pytest
  - pytest-cov
  - codecov
