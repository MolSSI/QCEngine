name: test
channels:
  - psi4/label/dev
  - conda-forge
dependencies:
  - psi4=1.6
  #- blas=*=mkl  # not needed but an example of disuading solver from openblas and old psi
  - libblas=*=*mkl

    # Core
  - python
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental >=0.28.0
  - pydantic>=1.0.0
  - msgpack-python
  - pint>=0.24.1
  - numpy=1.26

    # Testing
  - pytest
  - pytest-cov
  - codecov
