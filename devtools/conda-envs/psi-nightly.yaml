name: test
channels:
  - psi4/label/dev
  - conda-forge
dependencies:
  - psi4=1.6
  - blas=*=mkl  # not needed but an example of disuading solver from openblas and old psi

    # Core
  - python
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental >=0.26.0
  - pydantic>=2.0.0
  - msgpack-python
  - numpy=1

    # Testing
  - pytest
  - pytest-cov
  - codecov
