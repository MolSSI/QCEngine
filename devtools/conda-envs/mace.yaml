name: mace
channels:
  - conda-forge
dependencies:
    # Core
  - python
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental >=0.12.0
  - pydantic>=2.0.0
  - pydantic-settings

  # mace deps
  - pymace=0.3.6
  - pytorch<2.6  # until pickling issue resolved

    # Testing
  - pytest
  - pytest-cov
  - codecov
