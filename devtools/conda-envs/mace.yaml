name: mace
channels:
  - conda-forge
dependencies:
    # Core
  - python
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental >=0.12.0
  - pydantic>=1.0.0

  # mace deps
  - pymace=0.3.6
  - numpy<2.0  # until conda package imposes

    # Testing
  - pytest
  - pytest-cov
  - codecov
