name: test
channels:
  - psi4
dependencies:
  - psi4=1.2

    # Core
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental >=0.2.1
  - pydantic >=0.18.1

    # Testing
  - pytest
  - pytest-cov

    # Pip depends
  - pip:
    - codecov
    - git+https://github.com/leeping/geomeTRIC#egg=geometric
