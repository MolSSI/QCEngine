name: test
channels:
  - psi4
dependencies:
  - psi4=1.2

    # Core
  - pyyaml
  - py-cpuinfo
  - psutil

    # Testing
  - pytest
  - pytest-cov

    # Pip depends
  - pip:
    - codecov
    - git+https://github.com/leeping/geomeTRIC#egg=geometric
