name: test
channels:
  - psi4
dependencies:
  - psi4=1.2

    # Core
  - pyyaml
  - py-cpuinfo
  - psutil
  - qcelemental

    # Testing
  - pytest
  - pytest-cov

    # Pip depends
  - pip:
    - codecov
    - pydantic
    - git+https://github.com/leeping/geomeTRIC#egg=geometric
